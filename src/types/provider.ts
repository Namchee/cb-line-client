/**
 * A `Map` which contains provider name mappings
 */
const provider = new Map<string, string>();

provider.set('line', 'line');
provider.set('wa', 'whatsapp');
provider.set('whatsapp', 'whatsapp');

export { provider };
